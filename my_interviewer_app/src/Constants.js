import UserInfo from "./UserInfo"

const Constants = {}

const numOfInterviewQuestionsToAsk = 3

var user = new UserInfo("", 0, 0)

var idList = []

var blob = ""

export default Constants;
var recordedAudioAnswer = ""

export default Constants
